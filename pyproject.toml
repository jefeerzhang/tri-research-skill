[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "tri-research-runtime"
version = "6.9.0"
description = "Shared Machine Backend runtime for the tri-research and serpapi skills"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }

[tool.setuptools.packages.find]
where = ["skills/tri-research/src"]
