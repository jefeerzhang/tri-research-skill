__pycache__/

# API keys（serpapi skill 会读取 skills/*/.env，严禁入库）
.env
skills/*/.env

# Node 工具链（commitlint / husky / changelog）
node_modules/
npm-debug.log*

# 临时文件（shell 调试 / 状态机临时目录）
.tmp-*

# setuptools editable install
*.egg-info/
build/
dist/

# 本地 Agent 设置与调试产物（含个人路径，不入库）
.qoder/
.qoder-cn/
.statamcp/
